# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/unidatax_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: hekaig24

# 服务器配置
server:
  port: 18082
    
# 开发环境日志配置
logging:
  level:
    root: info
    com.queryu: debug
    org.springframework.web: info
    org.springframework.security: debug

# 开发环境允许跨域
cors:
  allowed-origins: http://localhost:3008,http://localhost:3000
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# 自定义配置
app:
  # JWT配置
  jwt:
    secret: queryuTrinoFrontendSecretKey2024ForHS512AlgorithmMustBeLongerThan512BitsAtLeast64Characters
    expiration: 86400000 # 24小时
    
  # 外部服务配置
  external:
    trino:
      url: https://trino-http.test.unicom.local:16000
      username: admin
      password: "rs{=uzW$UZ4v{BR!"
      timeout: 60000